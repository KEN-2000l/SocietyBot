# Global Config file
# YAML format
# Configs independent of guilds


bot:
    prefix:                          # prefix that works everywhere
      - '-SB '
      - '-sb '
      - 'sb '
    reply_to_mentions: true          # whether mentioning the bot is a valid prefix
    ignore_self: false               # if true, actions from the bot itself not trigger commands
    message_cache: 100000            # how many messages to keep in the client-side cache
    intents: all                     # todo: changed intents based on this setting
    extension_dir: extensions        # the name of the folder (under the working directory) that contains all the extension packages

permissions:
    owner:
      - 

    admin:
      - 

    mod:
      - 

    owners:
      - 768493088607830058
      - 544104227866279946

    admins:
      - 

log_channels:
    debug:
    info:
    error:
    message_create:
    message_delete:
    message_update:
    reaction_add:
    reaction_remove:
    channel_create:
    channel_delete:
    channel_update:
    pins_update:
    intergrations_update:
    webhooks_update:
